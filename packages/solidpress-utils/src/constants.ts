// Using require to escape from importing issue reported by typing system.
// eslint-disable-next-line @typescript-eslint/no-var-requires
export const SOLIDPRESS_VERSION = require('../package.json').version